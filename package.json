{
  "name": "deepseek-harness-reasonix",
  "version": "0.1.0",
  "private": true,
  "description": "用 DeepSeek Harness 替换 DeepSeek-Reasonix 后端 Agent：桥接层 + dsh bundle 落地仓库",
  "license": "MIT",
  "workspaces": [
    "bridge",
    "bundle-reasonix",
    "launcher"
  ],
  "scripts": {
    "build": "npm run build --workspace bridge && npm run build --workspace bundle-reasonix && npm run build --workspace launcher",
    "check": "npm run check --workspace bridge && npm run check --workspace bundle-reasonix && npm run check --workspace launcher"
  },
  "engines": {
    "node": ">=16"
  },
  "devDependencies": {
    "@types/node": "^16.18.126",
    "typescript": "^5.3.3"
  }
}
